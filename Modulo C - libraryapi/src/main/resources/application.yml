spring:
  application:
    name: libraryapi
  datasource:
    url: jdbc:postgresql://localhost:5435/library
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true # Mostra o Sql No terminal
    hibernate:
      ddl-auto: update # Atualiza/Cria o banco automaticamente baseado no código